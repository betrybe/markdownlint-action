name: markdownlint
description: Lint Markdown files
inputs:
  files:
    description: 'Files to lint'
    required: true
    default: '**/*.md'
  token:
    description: 'GitHub token'
    required: true
runs:
  using: 'node16'
  main: 'dist/index.mjs'
